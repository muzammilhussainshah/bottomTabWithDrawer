import React, { useEffect, useState } from 'react';
import { View, Text, } from 'react-native';
const KMP = ({ navigation }) => {
  return (
    <View style={{ flex: 1,  }}>
      <Text>KMP</Text>
    </View>
  );
};

export default KMP;
